apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    app: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - name: app-metrics
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
