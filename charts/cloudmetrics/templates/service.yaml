apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api-service
  labels:
    app: {{ .Release.Name }}-api
spec:
  selector:
    app: {{ .Release.Name }}-api
  ports:
    - name: app-metrics
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
