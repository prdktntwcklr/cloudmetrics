apiVersion: v1
kind: Service
metadata:
  name: cloudmetrics-service
  labels:
    app: cloudmetrics
spec:
  selector:
    app: cloudmetrics
  ports:
    - name: app-metrics
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
